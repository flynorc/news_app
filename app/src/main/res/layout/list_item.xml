<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/activity_horizontal_margin"
    android:layout_margin="@dimen/activity_horizontal_margin"
    android:background="@color/listItemBackground">


    <TextView
        android:id="@+id/article_title"
        tools:text="Article title"
        style="@style/list_item_title" />

    <TextView
        android:id="@+id/article_section_label"
        android:text="section: "
        style="@style/standard_text"
        android:layout_below="@+id/article_title"/>

    <TextView
        android:id="@+id/article_section"
        tools:text="article section"
        style="@style/standard_text"
        android:layout_toRightOf="@+id/article_section_label"
        android:layout_below="@+id/article_title"/>

    <TextView
        android:id="@+id/article_author_label"
        android:text="By: "
        style="@style/standard_text"
        android:layout_below="@+id/article_section"/>

    <TextView
        android:id="@+id/article_author"
        tools:text="author name"
        style="@style/standard_text"
        android:layout_below="@+id/article_section"
        android:layout_toRightOf="@id/article_author_label"/>

    <TextView
        android:id="@+id/article_published"
        tools:text="published date"
        style="@style/standard_text_right"
        android:layout_below="@+id/article_author"
        android:layout_alignParentEnd="true"/>
</RelativeLayout>